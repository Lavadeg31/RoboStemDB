{
  "name": "robotevents-firebase-sync",
  "version": "1.0.0",
  "description": "GitHub Actions workflow to sync RobotEvents API data to Firebase",
  "main": "scripts/sync.js",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "robotevents",
    "firebase",
    "sync",
    "github-actions"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}

